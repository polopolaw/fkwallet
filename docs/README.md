# FKWallet Documentation

This directory contains the documentation for the FKWallet Laravel package.

## Running Documentation Locally

To run the Docusaurus documentation locally:

```bash
cd docs
npm install
npm start
```

The documentation will be available at `http://localhost:3000`.

## Building Documentation

To build the documentation for production:

```bash
npm run build
```

The built files will be in the `build/` directory.

## Documentation Structure

- `index.md` - Main documentation index
- `quickstart.md` - Quick start guide
- `installation.md` - Installation instructions
- `configuration.md` - Configuration guide
- `usage.md` - Usage examples
- `api-reference.md` - Complete API reference
- `dto-reference.md` - DTO reference
- `exceptions.md` - Exception handling
- `webhooks.md` - Webhook handling
- `testing.md` - Testing guide

